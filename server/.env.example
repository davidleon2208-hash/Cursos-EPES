# Porta do servidor
PORT=3001

# Modo de execução
NODE_ENV=development

# Configuração SMTP (opcional - usa Ethereal para testes se não configurado)
# SMTP_HOST=smtp.seuservidor.com
# SMTP_PORT=587
# SMTP_USER=seu-email@seuservidor.com
# SMTP_PASS=sua-senha
# SMTP_SECURE=false

# Email remetente
FROM_EMAIL=noreply@pet-saude.br
